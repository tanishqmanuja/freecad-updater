# FreeCAD Updater

Helper to quickly update FreeCAD to the latest weekly release.

## Usage

Just drop the `Update.exe` file into a new directory and run it.
The script will automatically download and install the latest FreeCAD weekly release.
